{
  "name": "moon-v2",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:local": "vite build --mode local",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^12.3.0",
    "three": "0.160.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^5.4.0"
  }
}